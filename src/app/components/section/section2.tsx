import SectionWrapper from "../SectionWrapper";

export default function Section2() {
  return (
    <SectionWrapper className="w-full max-w-[430px] h-screen mx-auto bg-[#FFFACD] flex items-center justify-center text-black">
      Second
    </SectionWrapper>
  );
}
