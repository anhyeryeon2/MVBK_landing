import SectionWrapper from "../SectionWrapper";

export default function Section5() {
  return (
    <SectionWrapper className="w-full max-w-[430px] h-screen mx-auto bg-[#D8BFD8] flex items-center justify-center text-black">
      Fifth
    </SectionWrapper>
  );
}
