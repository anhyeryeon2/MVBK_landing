import SectionWrapper from "../SectionWrapper";

export default function Section4() {
  return (
    <SectionWrapper className="w-full max-w-[430px] h-screen mx-auto bg-[#B0E0E6] flex items-center justify-center text-black">
      Fourth
    </SectionWrapper>
  );
}
